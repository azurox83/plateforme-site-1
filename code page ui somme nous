<!doctype html>
<html lang="fr">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>La Plateforme — Accueil</title>
  <link rel="stylesheet" href="css/style.css" />
</head>
<body>
  <header>
    <div class="container nav">
      <div class="logo">La Plateforme</div>
      <nav>
        <ul>
          <li><a href="index.html">Accueil</a></li>
          <li><a href="qui-sommes-nous.html">Qui sommes-nous ?</a></li>
          <li><a href="#formations">Formations</a></li>
          <li><a href="#contact">Contact</a></li>
        </ul>
      </nav>
    </div>
  </header>

  <main class="container">
    <section class="hero">
      <div class="left">
        <div class="eyebrow">La grande école du numérique</div>
        <h1>Des parcours de formation accessibles à tous</h1>
        <p class="lead">Développement web, cybersécurité, IA et plus — sans conditions de ressources ni diplômes.</p>
        <p>
          <a class="cta" href="#inscription">Tests & Pré-inscriptions</a>
        </p>
      </div>
      <aside class="card">
        <h3>Prochaines journées portes ouvertes</h3>
        <ul>
          <li>Marseille — 12 novembre 2025</li>
          <img src="https://www.accentfrancais.com/fr/media/197/marseille_1200x800.png"
           alt ="Image de Marseille" width="30%"/>
          <li>Paris — 19 novembre 2025</li>
          <img src="https://www.thetrainline.com/cms/media/1360/france-eiffel-tower-paris.jpg?mode=crop&width=1080&height=1080&quality=70"
           alt="Image de Paris" width="30%"/>
        </ul>
      </aside>
    </section>

    <section class="team-photo">
      <h3>Notre équipe</h3>
      <img src= "https://laplateforme.io/wp-content/uploads/2021/07/HP_visual_light.jpg"
       alt="Photo de l'équipe de La Plateforme" width="30%" />
    </section>

    </section>nos partenaires
    <section class="partners">
      <h3>Nos partenaires</h3>
      <ul>
        <li><img src="https://www.lemondedesartisans.fr/sites/lemondedesartisans.fr/files/illustrations/articles/pole-emploi-versus-france-travail.jpg" alt="Pôle Emploi" width="150"/></li>
        <li><img src="https://upload.wikimedia.org/wikipedia/commons/7/7c/Logo-CNFPT-2024-RVB.png" alt="CNFPT" width="150"/></li>
        <li><img src="https://institutnr.org/wp-content/uploads/2020/08/INR-logo-s.jpg" alt="Institut du Numérique Responsable" width="150"/></li>
      </ul>
  </main>

  <footer>
    © La Plateforme — Prototype simplifié
  </footer>

  <script src="js/script.js"></script>
</body>


  css 


  /* Untitled-1.css
    Minimal, responsive, accessible CSS starter
*/

/* Variables */
:root{
  --bg: #ffffff;
  --surface: #f7f7fb;
  --text: #0f172a;
  --muted: #6b7280;
  --accent: #2563eb;
  --accent-600: #1d4ed8;
  --success: #16a34a;
  --danger: #dc2626;
  --radius: 8px;
  --shadow: 0 6px 18px rgba(15,23,42,0.08);
  --max-width: 1100px;
  --gap: 1rem;
  --fluid: clamp(1rem, 2vw, 1.25rem);
  --mono: ui-monospace, SFMono-Regular, Menlo, Monaco, "Roboto Mono", "Segoe UI Mono", monospace;
  --sans: Inter, "Segoe UI", Roboto, "Helvetica Neue", Arial, system-ui, -apple-system;
}

/* Reset / sensible defaults */
*,
*::before,
*::after { box-sizing: border-box; }
html, body, #root { height: 100%; }
html { font-size: 16px; -webkit-text-size-adjust: 100%; }
body {
  margin: 0;
  font-family: var(--sans);
  line-height: 1.5;
  color: var(--text);
  background: var(--bg);
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
  -webkit-tap-highlight-color: transparent;
}

/* Utility helpers */
.container {
  width: min(100%, var(--max-width));
  margin-left: auto;
  margin-right: auto;
  padding-left: clamp(0.75rem, 2vw, 1.25rem);
  padding-right: clamp(0.75rem, 2vw, 1.25rem);
}

.visually-hidden {
  position: absolute !important;
  height: 1px; width: 1px;
  overflow: hidden;
  clip: rect(1px, 1px, 1px, 1px);
  white-space: nowrap;
  border: 0;
  padding: 0;
  margin: -1px;
}

/* Typography */
h1,h2,h3,h4,h5,h6{ margin:0 0 0.5rem 0; font-weight:600; color:var(--text); }
p{ margin:0 0 1rem 0; color:var(--muted); }
h1{ font-size:clamp(1.5rem, 3.5vw, 2.25rem); }
h2{ font-size:clamp(1.25rem, 2.8vw, 1.75rem); }
small{ font-size:0.875rem; color:var(--muted); }

/* Link */
a{ color:var(--accent); text-decoration: none; }
a:hover, a:focus{ text-decoration: underline; outline: none; }

/* Focus styles for accessibility */
:focus{
  outline: 3px solid color-mix(in srgb, var(--accent) 20%, transparent);
  outline-offset: 2px;
  border-radius: 4px;
}

/* Layout helpers */
.row {
  display: flex;
  gap: var(--gap);
  flex-wrap: wrap;
}
.col { flex: 1 1 0%; min-width: 0; }
.grid {
  display: grid;
  gap: var(--gap);
}
.grid-cols-2 { grid-template-columns: repeat(2, 1fr); }
.grid-cols-3 { grid-template-columns: repeat(3, 1fr); }

/* Card */
.card{
  background: var(--surface);
  border-radius: var(--radius);
  padding: clamp(0.75rem, 1.5vw, 1rem);
  box-shadow: var(--shadow);
  border: 1px solid rgba(15,23,42,0.04);
}

/* Buttons */
.btn{
  display: inline-flex;
  align-items: center;
  gap: 0.5rem;
  padding: 0.5rem 0.9rem;
  border-radius: 6px;
  font-weight: 600;
  cursor: pointer;
  border: none;
  background: var(--accent);
  color: white;
  transition: transform .06s ease, box-shadow .06s ease, background-color .08s ease;
}
.btn:hover{ background: var(--accent-600); transform: translateY(-1px); }
.btn:active{ transform: translateY(0); }
.btn:disabled{ opacity: .6; cursor: not-allowed; transform: none; }

/* Variant */
.btn-ghost{
  background: transparent;
  color: var(--accent);
  border: 1px solid rgba(37,99,235,0.12);
}
.btn-link{
  background: transparent; color: var(--accent); padding: 0; font-weight: 600;
}

/* Form controls */
.input, textarea, select {
  width: 100%;
  padding: 0.6rem 0.75rem;
  border-radius: 6px;
  border: 1px solid rgba(15,23,42,0.08);
  background: white;
  color: var(--text);
  font: inherit;
}
.input:focus, textarea:focus, select:focus {
  border-color: var(--accent);
  box-shadow: 0 0 0 4px color-mix(in srgb, var(--accent) 10%, transparent);
  outline: none;
}

/* Form layout */
.form-row { display:flex; gap: var(--gap); align-items: center; flex-wrap:wrap; }
.form-row > label { min-width: 10ch; color:var(--muted); }

/* Small components */
.kv {
  display:flex; justify-content:space-between; gap:1rem;
}
.badge {
  display:inline-block; padding:0.15rem 0.5rem; border-radius:999px; background:rgba(15,23,42,0.06); font-size:0.75rem;
}

/* Tables (simple) */
.table {
  width:100%;
  border-collapse: collapse;
  background: white;
  overflow: auto;
}
.table th, .table td {
  text-align: left; padding: 0.6rem; border-bottom: 1px solid rgba(15,23,42,0.04);
}

/* Responsive helpers */
@media (max-width: 720px) {
  .grid-cols-2 { grid-template-columns: 1fr; }
  .grid-cols-3 { grid-template-columns: 1fr; }
  .row { flex-direction: column; }
}

/* Dark mode support (optional) */
@media (prefers-color-scheme: dark) {
  :root{
     --bg: #efe3e3;
     --surface: #07192a;
     --text: #ff9500;
     --muted: #ff921d;
     --shadow: 0 8px 30px rgba(2,6,23,0.6);
  }
  body { background: var(--bg); color: var(--text); }
  .card { background: linear-gradient(180deg, rgba(255,255,255,0.02), rgba(255,255,255,0.01)); border: 1px solid rgba(255,255,255,0.04); }
  .input, textarea, select { background: rgba(255,255,255,0.02); border: 1px solid rgba(255,255,255,0.04); color: var(--text); }
}

/* Small clearfix */
.clearfix::after { content: ""; display: table; clear: both; }

/* End of file */

</html>
